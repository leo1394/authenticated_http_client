# authenticated_http_client Copilot Context

Use the singleton `AuthenticatedHttpClient.getInstance()`. Generate APIs via `factory`; pass path params as a map. Use `MOCK` for local JSON assets, `SILENT` for no auth-navigation side effects, `UP` for upload, `DOWN` for download. Prefer `HttpHeadersInterceptor` for shared headers and `RouterHelper.init` for unauthorized navigation.
